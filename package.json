{
  "name": "feature-flag-monorepo",
  "private": true,
  "packageManager": "pnpm@9",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"pnpm -C apps/backend dev\" \"pnpm -C apps/frontend dev\"",
    "dev:backend": "pnpm -C apps/backend dev",
    "dev:frontend": "pnpm -C apps/frontend dev",
    "build": "pnpm -C apps/backend build && pnpm -C apps/frontend build",
    "start:backend": "pnpm -C apps/backend start"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
